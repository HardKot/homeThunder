version: "3"

services:
    database:
        image: postgres:latest
        ports:
            - "5432:5432"
        environment:
            - POSTGRES_USER=user
            - POSTGRES_PASSWORD=123
            - POSTGRES_DB=admin
